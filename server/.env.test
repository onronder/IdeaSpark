# Test Environment Configuration
NODE_ENV=test

# Database (using test database on different port)
DATABASE_URL=postgresql://test:test@localhost:5433/ideaspark_test

# Redis (using database 1 for tests)
REDIS_URL=redis://localhost:6379/1

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Server Configuration
PORT=4001
API_VERSION=v1

# Logging (silent in tests)
LOG_LEVEL=silent

# Rate Limiting (relaxed for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Sentry (disabled for tests)
SENTRY_DSN=

# Email (disabled for tests)
SMTP_HOST=localhost
SMTP_PORT=1025
EMAIL_FROM=test@ideaspark.com

# OpenAI (stubbed for tests)
OPENAI_API_KEY=test-key
OPENAI_MODEL=gpt-4

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:8081

# Security
SESSION_SECRET=test-session-secret
COOKIE_SECRET=test-cookie-secret